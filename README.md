# Лабораторная работа №8: Статические и динамические библиотеки

## 📋 Описание проекта
Проект демонстрирует создание статической и динамической библиотек для работы с бинарными данными в языке C++.

## 🏗️ Структура проекта

### 1. Статическая библиотека (`StaticLibNew`)
- **Файлы:** `BinaryIO.h`, `BinaryIO.cpp`
- **Пространство имен:** `BinaryIO`
- **Функции:**
  - `serializeInt32()` - сериализация целых чисел
  - `serializeDouble()` - сериализация чисел с плавающей точкой
  - `serializeString()` - сериализация строк
  - Соответствующие функции десериализации
- **Результат:** `StaticLibNew.lib`

### 2. Динамическая библиотека (`BinaryIODynamicLib`)
- **Файлы:** `BinaryFileHandler.h`, `BinaryFileHandler.cpp`, `BinaryIOExport.h`
- **Класс:** `BinaryFileHandler` в пространстве имен `BinaryIO`
- **Методы:**
  - `writeToFile()` - запись бинарных данных в файл
  - `readFromFile()` - чтение бинарных данных из файла
- **Результат:** `BinaryIODynamicLib.dll` + `BinaryIODynamicLib.lib`

### 3. Клиентское приложение (`BinaryIOClientApp`)
- **Файл:** `main.cpp`
- **Зависимости:** использует обе библиотеки
- **Демонстрация:** совместной работы статической и динамической библиотек

## 🚀 Сборка и запуск

### Требования:
- Visual Studio 2019/2022
- Платформа: x64
- Конфигурация: Debug

### Порядок сборки:
1. Собрать `StaticLibNew` (F7) → создается `StaticLibNew.lib`
2. Собрать `BinaryIODynamicLib` (F7) → создаются `BinaryIODynamicLib.dll` и `.lib`
3. Собрать `BinaryIOClientApp` (F7) → создается `BinaryIOClientApp.exe`
4. Запустить (Ctrl+F5)

### Настройки клиентского приложения:
- **Дополнительные каталоги включаемых файлов:**
